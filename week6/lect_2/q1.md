```python
llm = init_chat_model('gpt-3.5-turbo',model_provider='openai',temperature=0.7)
```
    - temperature is a parameter that controls the randomness/creativity of the output
```py
llm.invoke("Hello, how are you?")
```
    - this will return a response from the LLM
    - it is not a string , but it is an object of AIMessage, which has a content attribute,


```
inputs=[
    "Explain langchain in one line",
    "Explain what is LLM",
    "Explain what is vector embedding."
]

responses=llm.batch(inputs)
for res in responses:
    print(res.content)
```